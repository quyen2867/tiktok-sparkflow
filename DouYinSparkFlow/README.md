# DouYinSparkFlow

Thư mục code chính. Cách chạy xem README ở ngoài repo.

- `core/` — trình duyệt, đăng nhập, đọc inbox, gửi tin, lịch.
- `webui/` — dashboard FastAPI.
- `utils/` — config, log.
- `scripts/` — script chạy macOS, kiểm tra selector.
- `tests/` — unit test + fixture offline.
